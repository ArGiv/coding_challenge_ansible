apic:
  hosts:
    sandboxapicdc:
      ansible_host: "sandboxapicdc.cisco.com"

  vars:
    ansible_user: "admin"
    # public sandbox password
    ansible_password: "!v3G@!4@Y"
    ansible_httpapi_use_ssl: True
    ansible_httpapi_validate_certs: False
    ansible_python_interpreter: auto